# Paint
CANVAS = [0,0,0,0] # x, y, w, h: dimensions of canvas
PEN = [0,0] # x, y: position of pen button
FILL = [0,0] # x, y: position of fill button
PALETTE_POS = [0,0] # x, y: topleft corner of color palette
PALETTE_DIMS = []


# Skribbl

# Pictionary

# Image processing parameters
COLOR_THRESHOLD = 50 # maximum distance between two colors for them to be considered similar
PIXEL_THRESHOLD = 500 # how many similar pixels are needed for a contour to be contructed for the region